# Roadmap

## Near‑Term (Polish)
- Optional line‑clear burst effect (lightweight particles).
- Subtle background depth (gradient/noise layer).

## Mid‑Term (UX)
- Minimal replay/debug log for timing analysis.
- Optional accessibility tweaks (larger HUD, reduced motion).

## Long‑Term (Optional)
- Theming presets (still no Settings UI; choose a single default per build).
